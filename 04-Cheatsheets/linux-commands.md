---
tags: [cheatsheet, linux, commands]
module: Linux Fundamentals
---

# Linux Commands Cheatsheet

## System / Network Info
| Command | Description |
|---|---|
| `whoami` | Displays current username. |
| `id` | Returns user's identity. |
| `hostname` | Sets or prints the name of the current host system. |
| `uname` | Prints basic information about the OS name and system hardware. |
| `pwd` | Returns working directory name. |
| `ifconfig` | Assign/view an address to a network interface, or configure network interface parameters. |
| `ip` | Show or manipulate routing, network devices, interfaces, and tunnels. |
| `netstat` | Shows network status. |
| `ss` | Investigate sockets. |
| `ps` | Shows process status. |
| `who` | Displays who is logged in. |
| `env` | Prints environment, or sets and executes a command. |
| `lsblk` | Lists block devices. |
| `lsusb` | Lists USB devices. |
| `lsof` | Lists opened files. |
| `lspci` | Lists PCI devices. |
| `which` | Returns the path to the file/link that would be executed. |
| `find` | Finds files and folders; supports filtering results. |

## File & Directory Management
| Command | Description |
|---|---|
| `cd` | Move through directories. |
| `touch` | Create empty files, or update timestamps on existing files without modifying contents. |
| `mkdir` | Create a directory. Use `-p` to create parent directories as needed. |
| `mv` | Move and/or rename files and directories. |

![[Pasted image 20260909135406.png]]